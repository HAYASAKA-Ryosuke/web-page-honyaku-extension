{
  "name": "translator-mailer-chrome-ext",
  "version": "1.3.1",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:chrome": "vite build --mode chrome",
    "build:firefox": "node scripts/build-firefox.js && node scripts/generate-firefox-manifest.js",
    "build:zip": "vite build && node scripts/build-zip.js",
    "build:zip:chrome": "vite build --mode chrome && node scripts/build-zip.js chrome",
    "build:zip:firefox": "node scripts/build-firefox.js && node scripts/generate-firefox-manifest.js && node scripts/build-zip.js firefox",
    "dev": "vite",
    "preview": "vite preview",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx --fix",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.1",
    "@types/node": "^20.0.0",
    "@types/webextension-polyfill": "^0.12.4",
    "chrome-types": "^0.1.384",
    "esbuild": "^0.24.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vitest": "^3.0.0"
  }
}
